import { MantineThemeOverride } from '@mantine/core'

export const baseTheme: MantineThemeOverride = {
  colorScheme: 'light',
  fontFamily: ['Poppins', 'sans-serif', 'Indie Flower', 'cursive'].join(','),
}
